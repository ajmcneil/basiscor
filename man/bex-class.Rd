% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expansion.R
\docType{class}
\name{bex-class}
\alias{bex-class}
\alias{show,bex-method}
\alias{udptrans,bex-method}
\title{Class of Basis Expansion Objects}
\usage{
\S4method{show}{bex}(object)

\S4method{udptrans}{bex}(x, u)
}
\arguments{
\item{object}{an object of class \linkS4class{bex}.}

\item{x}{an object of class \linkS4class{bex}.}

\item{u}{a two-column matrix with values in `(0, 1)`.}
}
\description{
A `bex` object packages the pair of basis-function expansions `g(u) =
sum_i alphag[i] * basisfunc(u, i, type)` and `h(u) = sum_i alphah[i] *
basisfunc(u, i, type)` that jointly maximize the generalized Spearman
correlation `maxcor` between the two margins of a bivariate sample (see
[basisexpand()]), together with the corresponding \pkg{udp}
uniform-distribution-preserving transformations `Tg`, `Th` -- `Tg(u) =
F(g(u))`, `Th(u) = F(h(u))` -- built from them.
}
\section{Functions}{
\itemize{
\item \code{show(bex)}: Show method for bex objects.

\item \code{udptrans(bex)}: Apply the pair of udp transformations `Tg`, `Th` of
a bex object to the two columns of a bivariate matrix, `cbind(udptrans(x@Tg,
u[, 1]), udptrans(x@Th, u[, 2]))`. Unlike a single \linkS4class{udp}
transformation, which acts on a vector, `u` here must be a two-column
matrix since the two columns get different transformations. Use
`udp::boundaryadjust()` on the result if values strictly inside `(0, 1)`
are needed.

}}
\section{Slots}{

\describe{
\item{\code{type}}{character; `"legendre"` or `"cosine"`.}

\item{\code{maxcor}}{numeric; the maximal generalized Spearman correlation attained.}

\item{\code{alphag,alphah}}{numeric; the weights of the two expansions.}

\item{\code{Tg,Th}}{objects of class \code{udp} (see `udp::udplegendre_sum()` /
`udp::udpcosine_sum()`) built from `alphag`, `alphah`.}
}}

\seealso{
[basisexpand()] to construct one.
}
